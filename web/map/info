function viewInfoWindow(){
    // marker.addListener('click', function() {   
      //     infowindow.open(map, marker);
      // });
}



$(loginmodal).show();
$('.modal-backdrop').show();
$(desc).innerHTML = "You need to be logged as a BPI admin to view infestation information.";